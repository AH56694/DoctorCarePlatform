[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".", "python-service"]

[tool.pyright]
extraPaths = ["python-service"]
include = ["backend", "python-service", "tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
