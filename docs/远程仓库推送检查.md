# 远程仓库推送检查

检查日期：2026-09-18。仓库：`AH56694/DoctorCarePlatform`。

## 检查范围

- 已成功拉取的 `origin/main`（本轮推送前为 `c81e761`）、该分支可达的 Git 历史、本地待推送提交 `481d24f` 和本轮待提交源码。
- 检查已跟踪文件、历史文件对象、配置中的凭据字面量、常见服务令牌及私钥特征；历史 SQLite 数据库只在内存中读取表结构和记录数量。
- 正则扫描结果人工复核。报告不复制手机号、密码哈希、短信内容或问诊正文。
- 此次检查不是完整的数据泄露证明或绝对安全保证；不覆盖 GitHub 不可达对象、隐藏的 PR 引用、外部 fork、历史下载副本、Actions 日志与发布附件。

## 发现的问题

### 高优先级：历史提交仍保留业务数据库

`local.db` 在 `464e548` 首次加入，在 `250d16d` 更新，在 `7ac1116` 从当时的文件树删除。最新分支文件树虽然已经没有它，历史提交仍能恢复两个 SQLite 文件版本。

其中较新版本（Git 文件对象 `c6febd898603`）包含：

| 数据类型 | 数量 |
| --- | ---: |
| 用户账号 | 8 |
| 非空手机号 / 密码哈希 | 各 8 |
| AI 会话 / AI 消息 | 26 / 52 |
| 普通聊天消息 | 4 |
| 短信通知 | 12 |
| 管理操作日志 | 16 |

较旧版本（`981b11a49a7c`）也有 7 个账号及密码哈希、4 条普通聊天消息和 11 条短信通知。数据库中部分值具有演示特征，但无法据此认定所有手机号、账号和问诊内容均为虚构；这些运行数据不应进入代码仓库。

模型配置中的 `api_key_ref` 为环境变量名称，本次未发现数据库含符合扫描特征的真实服务令牌。密码哈希仍属于需要保护的数据，不能因不是明文而视为可公开。

### 低优先级：无关文档和可重新生成的产物

- 远程当前版本的 `python-service/1.docx` 内容是《中华人民共和国个人所得税法》，与项目无关，源码没有引用。
- 本地生成的根目录 `overview.html` 是离线页面预览，可由 `frontend/scripts/export-overview.mjs` 重新生成。本轮保留源码、插画和导出脚本，不将生成文件加入 Git。

## 本轮处理

- 为 SQLite 数据库及辅助文件、数据库转储、备份目录和常见私钥文件增加 Git 与 Docker 忽略规则，保留 SQL 迁移和建表脚本。
- 从 Git 索引移除 `python-service/1.docx`，保留本地原文件；增加对应忽略规则。
- 忽略生成的 `overview.html`，保留本地交付文件和生成方式。
- 未发现远程当前文件树跟踪真实 `.env`、虚拟环境、`node_modules`、FAISS 索引或模型权重。
- 本轮凭据扫描的候选项已核对：脱敏占位符、环境变量插值、测试样例和开发演示配置，不是已确认泄露的真实 API 密钥。

## 仍待处理的历史风险

以上常规提交不能清除旧提交中的 `local.db`。彻底处理需要单独改写远程历史：

1. 核对数据库账号是否曾用于实际环境；如有使用或密码复用，重置相关密码并失效现存会话。
2. 在受控位置备份仓库，通知协作者暂停推送；使用独立镜像克隆，从所有相关分支和标签中移除 `local.db`（可同时移除无关的 `python-service/1.docx`）。
3. 验证清理后的引用、历史对象和代码文件一致性，再按明确授权更新远程历史。此操作会改变后续提交 ID，需要协作者重新克隆或同步，不能直接进行普通合并。
4. 如仓库曾公开或内容已被下载，历史改写不能收回已有副本；必要时按托管平台流程处理缓存引用或副本。

本轮未执行远程历史改写或强制推送。历史数据库风险仍然存在。
